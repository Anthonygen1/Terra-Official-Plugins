apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}-initdb"
  labels:
    kuiper.juno-innovations.com/kuiper-instance: "{{ .Release.Name }}"
data:
  init.sql: |
    CREATE TABLE IF NOT EXISTS customers (
      id SERIAL PRIMARY KEY,
      name TEXT NOT NULL,
      company TEXT,
      email TEXT,
      status TEXT NOT NULL DEFAULT 'lead',
      created_at TIMESTAMPTZ NOT NULL DEFAULT now()
    );

    CREATE TABLE IF NOT EXISTS deals (
      id SERIAL PRIMARY KEY,
      customer_id INT NOT NULL REFERENCES customers(id),
      title TEXT NOT NULL,
      value_usd NUMERIC(12,2) NOT NULL,
      stage TEXT NOT NULL DEFAULT 'prospecting',
      close_date DATE,
      created_at TIMESTAMPTZ NOT NULL DEFAULT now()
    );

    CREATE TABLE IF NOT EXISTS notes (
      id SERIAL PRIMARY KEY,
      customer_id INT NOT NULL REFERENCES customers(id),
      body TEXT NOT NULL,
      author TEXT NOT NULL DEFAULT 'anthony',
      created_at TIMESTAMPTZ NOT NULL DEFAULT now()
    );

    INSERT INTO customers (name, company, email, status) VALUES
     ('Dana Reeve','Lumon Industries','dana@lumon.example','active'),
     ('Marcus Chen','Wayfinder Studios','marcus@wayfinder.example','active'),
     ('Priya Nair','Helixon Bio','priya@helixon.example','lead'),
     ('Tom Alvarez','Redline Post','tom@redline.example','churned'),
     ('Sofia Brandt','Northcap Finance','sofia@northcap.example','active');

    INSERT INTO deals (customer_id, title, value_usd, stage, close_date) VALUES
     (1,'Render farm expansion',48000,'negotiation','2026-08-15'),
     (1,'GPU workstation seats',12500,'closed_won','2026-06-30'),
     (2,'Pipeline migration',75000,'proposal','2026-09-01'),
     (3,'HPC pilot',30000,'prospecting',NULL),
     (4,'Storage upgrade',18000,'closed_lost','2026-05-20'),
     (5,'Compliance workloads',95000,'negotiation','2026-08-30');

    INSERT INTO notes (customer_id, body) VALUES
     (1,'Wants on-prem only, security review passed.'),
     (2,'Waiting on budget approval from CFO.'),
     (3,'Intro call went well, send pilot scope.'),
     (5,'Needs SOC2 report before signing.');
